goog.declareModuleId('coreui.layout.TabParams');

/**
 * Constant parameters for tab sizing.
 * @enum {number}
 */
export const TabParams = {
  MIN_WIDTH: 100, // minimum pixel width of a tab
  MAX_WIDTH: 155, // maximum pixel width of a tab
  CLOSE_WIDTH: 50, // the width of the close button + extra paddings
  PLUS_MENU_WIDTH: 60 // the width of the + button and the menu button
};
