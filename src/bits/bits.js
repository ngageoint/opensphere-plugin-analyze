goog.module('bits');
goog.module.declareLegacyNamespace();

/**
 * @define {string} The root path to bits-internal
 */
const ROOT = goog.define('bits.ROOT', '../opensphere-plugin-analyze/');

exports = {
  ROOT
};
